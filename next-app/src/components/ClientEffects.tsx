"use client";

/**
 * ClientEffects —— 布局级客户端特效挂载点（占位）。
 *
 * 后续承载（迁移自旧 Vite 项目，ROADMAP「'use client' 边界原则」第 3 条）：
 * - MusicPlayer 音乐播放器
 * - 光标特效 / 星星背景 / Lenis 平滑滚动
 * - ContextMenu 自定义右键菜单
 * - DebugProtection
 * - 主题切换（dark/light）
 *
 * 当前为空组件，保证 layout 挂载结构先行稳定。
 */
export default function ClientEffects() {
  return null;
}
